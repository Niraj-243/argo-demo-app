apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "argo-demo-chart.fullname" . }}
  labels:
    app: {{ include "argo-demo-chart.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "argo-demo-chart.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "argo-demo-chart.name" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 80

