apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "argo-demo.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "argo-demo.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "argo-demo.name" . }}
    spec:
      containers:
        - name: {{ include "argo-demo.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 80

