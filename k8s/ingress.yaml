apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argo-demo
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: argo-demo
                port:
                  number: 80

